# AGENTS

## Repo Snapshot
- Java 11 RuneLite Plugin Hub project built with Gradle.
- Main plugin entry: `com.geassistant.GeAssistantPlugin`
- Dev runner configured in Gradle: `com.geassistant.GeAssistantPluginTest`

## Verified Commands
- `./gradlew.bat test` invokes the JUnit 4 test task.
- `./gradlew.bat build` assembles the plugin and invokes the test task.
- `./gradlew.bat run` launches the configured developer-mode RuneLite test runner.
- `./gradlew.bat shadowJar` builds the fat JAR declared in `build.gradle`.

## Repo-Local Workflows
- `.claude/commands/tdd.md`: test-first workflow for feature and bugfix work.
- `.claude/commands/debug.md`: root-cause-first debugging workflow.
- `.claude/commands/verify.md`: verification gate before claiming completion.
- `.claude/commands/code-review.md`: request a review pass before merge.
- `.claude/commands/parallel-agents.md`: split independent workstreams when tasks do not overlap.
- `.claude/commands/using-superpowers.md`: activate the repo's Superpowers skill-selection workflow before starting a task.
- `.claude/commands/tmux.md`: run interactive CLI tools through detached tmux sessions.
- `.claude/commands/mcp-cli.md`: discover and invoke MCP servers on demand through the `mcp` CLI.
- `.claude/commands/finish-branch.md`: complete a development branch with verify/merge/PR/cleanup flow.
- `.claude/commands/find-duplicates.md`: detect semantic duplicate functions and generate a consolidation report.
- `.clinerules/ge-assistant-conventions.md`: project-specific Java, RuneLite, logging, and testing conventions.

## TODO
- `.codex/environments/environment.toml` is marked autogenerated and exposes `npm run dev`, but this repo's verified command surface is Gradle-based. Do not rely on that action without re-validating it.
- `./gradlew.bat test` currently fails in `src/test/java/com/geassistant/GeOfferInsightBuilderTest.java` and `src/test/java/com/geassistant/WikiPriceCacheTest.java` because those tests reference missing `MarketSnapshot` and opportunity-scoring APIs.
